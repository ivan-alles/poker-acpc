This directory is prepared for interactive tests of agent gui.

Usage:

1. Update pom.xml with all necessary artifacts to run the bot.

1. run
   mvn validate

2. Create directory test-config and put a valid bot configuration there

3. Run run-agentgui.bat
